const IMAGE_PARAMS =`
    image.description,
    image.filename_disk,
    image.filename_download,
    image.filesize,
    image.id,
    image.location,
    image.metadata,
    image.title,
    image.type
`;

export {IMAGE_PARAMS}
